services:    data:        environment:            - SA_PASSWORD=${SQL_PASSWORD}        healthcheck:            test: /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P "${SQL_PASSWORD}" -Q "SELECT 1" || exit 1            interval: 30s            timeout: 10s            retries: 5            start_period: 60s                message:        environment:            - RABBITMQ_DEFAULT_USER=${RABBITMQ_USER}            - RABBITMQ_DEFAULT_PASS=${RABBITMQ_PASS}                identity:        image: dkisov/body4uhub-identity:latest                content:        image: dkisov/body4uhub-content:latest                marketplace:        image: dkisov/body4uhub-services:latest